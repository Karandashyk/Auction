<%= form_with(model: users_product, local: true) do |form| %>
  <% if users_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_product.errors.count, "error") %> prohibited this users_product from being saved:</h2>

      <ul>
      <% users_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :users_product_user_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id, id: :users_product_product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
